# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: zhrsanctions
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        ignoreCertErrors: true
        target:
          url: https://10.123.1.11:44300
          client: '100'
        credentials:
          username: env:FIORI_DEPLOY_USERNAME
          password: env:FIORI_DEPLOY_PASSWORD
        app:
          name: ZHR_SANCTIONS
          description: Sanctions and Violations
          package: ZDAC_HR
          transport: MD4K916093
        exclude:
          - /test/
